1)приложение JUnit_Spring:
В данном приложении создан класс, в котором есть поля и один, который должен считать,когда вызываются другие методы. В данном случае методы get и set.
В классе Car7Test:
1.Определяем порядок работы. Строка @RunWith(SpringJUnit4ClassRunner.class) - указывает,что в начале(перед началом работы самого приложения) должен поработать Spring
2.Затем определяем местоположение контекста Spring(настройки для Spring)
3.Затем даем первую команду Spring - Создать свой контекст
4.Создаем объект тестируемого класса - Предполагалось, что объект будет виден во всем классе.Однако тогда нужно дважды создавать его(бин).Можно ли так?
5.Проверяем тут же, создался ли объект(бин)?
6.Так как стоит анотация @Before - это значит, что вызовов методов класса не было. Хотя странно, мы ведь вызвали перегруженный метод toString. - в методе public void calls()